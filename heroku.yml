build:
  docker:
    web: Dockerfile

env:
  NODE_VERSION: "20"
  NODE_OPTIONS: "--max-old-space-size=4096"
  BUILD_STORYBOOK: "true"
  COPY_TO_DIST: "false"

formation:
  web:
    quantity: 1
    size: basic 